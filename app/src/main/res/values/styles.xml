<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="SeekBarStyle">
        <item name="colorAccent">#ffffff00</item>
    </style>

    <style name="checkBoxStyle">
        <item name="colorControlActivated">@color/design_textinput_error_color</item>
        <item name="colorControlNormal">@color/design_textinput_error_color</item>
    </style>

    <style name="alertDialog">
        <item name="android:textColorPrimary">@color/text_color</item>
        <item name="android:textColor">@color/bursh_ok_but</item>
        <item name="android:background">@color/image_bg</item>
        <item name="android:textColorAlertDialogListItem">@color/text_color</item>
    </style>
    <style name="alertDialogreset">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/design_textinput_error_color</item>
        <item name="android:background">@color/image_bg</item>
        <item name="android:textColorAlertDialogListItem">@color/black</item>
    </style>

</resources>
